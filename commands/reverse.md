---
description: "Reverse-engineer a prompt from an existing image"
argument-hint: '"path_or_last" [--generate] [--model flash|pro] [--output path]'
---

Run the reverse script:
```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/reverse.mjs" $ARGUMENTS
```
