---
description: "Edit an existing image via Gemini"
argument-hint: '"path_or_last" "instructions" [--model flash|pro] [--output path] [--ref path ...]'
---

Run the edit script:
```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/edit.mjs" $ARGUMENTS
```
